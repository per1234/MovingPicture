# Notes

The timestamp on the first revision of the sketch I have is 2013-10-26, so the project likely started around that date.

The revision control was done by periodically creating copies of the sketch. I retroactively migrated those copies to Git commits, with the title "update".